{% extends 'inside.html.twig' %}

{% block title %}Modifier Demande{% endblock %}

{% block main %}
   <div class="card">
        <div class="card-header">
           <h3>Modifier la Demande</h3>
        </div>
        <div class="card-body">
        {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
        <div class="mt-4 row d-flex flex-column align-items-center">
            <div class="mb-3">
                {{ form_row(form.titre) }}
            </div>
            <div class="mb-3">
                {{ form_row(form.description) }}
            </div>
            <div class="row">
                <div class="mb-3 col">
                    {{ form_row(form.date_creation) }}
                </div>
                <div class="mb-3 col">
                    {{ form_row(form.Source) }}
                </div>
            </div>    
            <div class="row">
                <div class="mb-3 col">
                    {{ form_row(form.type) }}
                </div>
                <div class="mb-3 col">
                    {{ form_row(form.canal) }}
                </div>
                <div class="mb-3 col">
                    {{ form_row(form.statut) }}
                </div>
            <div>
            <div class="my-4 col-md-12 d-flex justify-content-between">
                <button type="submit" class="btn btn-primary me-1">Enregistrer</button>
                <a href="{{ path('app_assistance_index') }}" class="btn btn-secondary w-50 me-1">Retour</a>
            </div>
        </div>
        {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
