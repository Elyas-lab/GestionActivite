{% extends 'inside.html.twig' %}

{% block title %}Demande{% endblock %}

{% block main %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numéro</th>
                <td>{{ demande.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ demande.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ demande.description }}</td>
            </tr>
            <tr>
                <th>Date_insertion</th>
                <td>{{ demande.date_creation|date('d-m-Y H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_index') }}">back to list</a>

{% endblock %}

