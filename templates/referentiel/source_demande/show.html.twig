{% extends 'inside.html.twig' %}


{% block title %}SourceDemande{% endblock %}

{% block main %}
    <h1>SourceDemande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ source_demande.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ source_demande.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_source_demande_index') }}">back to list</a>

    <a href="{{ path('app_source_demande_edit', {'id': source_demande.id}) }}">edit</a>

    {{ include('referentiel/source_demande/_delete_form.html.twig') }}
{% endblock %}
