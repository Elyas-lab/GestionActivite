{% extends 'inside.html.twig' %}


{% block title %}TypeDemande{% endblock %}

{% block main %}
    <h1>TypeDemande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_demande.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_demande.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_demande.description }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ type_demande.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_demande_index') }}">back to list</a>

    <a href="{{ path('app_type_demande_edit', {'id': type_demande.id}) }}">edit</a>

    {{ include('referentiel/type_demande/_delete_form.html.twig') }}
{% endblock %}
