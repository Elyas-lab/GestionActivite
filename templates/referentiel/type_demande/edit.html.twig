{% extends 'inside.html.twig' %}

{% block title %}Modifier un Type de Demande{% endblock %}

{% block main %}
<div class="card">
    <div class="card-header">
        Modifier un Type de Demande
    </div>
    <div class="card-body">
        {{ form_start(form) }}

        <div class="mt-4 row d-flex flex-column align-items-center">
            <div class="col-md-10">
                <!-- Nom -->
                <div class="mb-3">
                    {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Nom du type de demande'}}) }}
                    {{ form_errors(form.nom) }}
                </div>

                <!-- Description -->
                <div class="mb-3">
                    {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Description du type de demande', 'rows': '5'}}) }}
                    {{ form_errors(form.description) }}
                </div>

                <!-- Actif -->
                <div class="mb-3 form-check">
                    {{ form_widget(form.isActive, {'attr': {'class': 'form-check-input'}}) }}
                    {{ form_label(form.isActive, 'Actif', {'label_attr': {'class': 'form-check-label'}}) }}
                    {{ form_errors(form.isActive) }}
                </div>
            </div>

            <!-- Buttons -->
            <div class="my-4 col-md-10 d-flex justify-content-between">
                <a href="{{ path('app_type_index') }}" class="btn btn-secondary ">Retour</a>
                <button type="submit" class="btn btn-success ">Enregistrer</button>
            </div>
        </div>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
