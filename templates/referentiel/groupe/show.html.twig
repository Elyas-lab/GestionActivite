{% extends 'inside.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block main %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ groupe.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupe_index') }}">back to list</a>

    <a href="{{ path('app_groupe_edit', {'id': groupe.id}) }}">Editer</a>
    <a href="{{ path('app_groupe_permission', {'id': groupe.id}) }}">Gerer les permissions</a>

    {{ include('referentiel/groupe/_delete_form.html.twig') }}
{% endblock %}
